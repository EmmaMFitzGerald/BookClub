<% if @book %>
<h1>Notes for <%= @book.title %>
<% end %>
<ul>
<% @comments.each do |comment|%>
  <li><%= comment.chapter %>: <%= comment.content %></li>
  <%= link_to "Edit Notes" , edit_comment_path(comment)%>
<% end %>
</ul>