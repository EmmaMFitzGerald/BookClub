<h1><%= @book.title %> </h1>
<p><%= @book.author %></p>
<p><%= @book.genre %></p>
<p><%= @book.blurb%></p>

<%=link_to "See all comments", books_comments_path(@book)%>

<%=link_to "Add new comment", new_book_comment_path(@book)%>

<p>Notes on this book<p>
<% @book.comments.each do |comment| %>
<%= comment.chapter %>
<%= comment.content %> 